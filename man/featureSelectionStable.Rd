% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featureSelection.R
\name{featureSelectionStable}
\alias{featureSelectionStable}
\title{Select Spatially and Group-wise Stable Genes}
\usage{
featureSelectionStable(
  object,
  control_group_value = "0",
  check_group_diff = TRUE,
  case_group_value = "1",
  max_group_logfc_cut = 0.1,
  mean_global_cut = 0.3,
  pct_in_cut = 0.05,
  min_domain_prop = 0.8,
  max_abs_logfc_cut = 0.2,
  cv_cut = 0.3,
  max_num_gene = 50,
  use_low = TRUE,
  n_cores = 1,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{spaDesign2} object.}

\item{control_group_value}{Character. Value indicating the control group (default "0").}

\item{check_group_diff}{Logical. Check expression difference between Control and Case (default TRUE).}

\item{case_group_value}{Character. Value indicating the case group (default "1").}

\item{max_group_logfc_cut}{Numeric. Maximum allowed absolute logFC between Control and Case (default 0.1).}

\item{mean_global_cut}{Numeric. Minimum global mean log-expression (default 0.3).}

\item{pct_in_cut}{Numeric. Minimum detection rate within a domain (default 0.05).}

\item{min_domain_prop}{Numeric. Minimum proportion of domains that must meet \code{pct_in_cut} (default 0.8).}

\item{max_abs_logfc_cut}{Numeric. Maximum allowed spatial logFC variance (default 0.2).}

\item{cv_cut}{Numeric. Maximum allowed Robust CV across domain means (default 0.3).}

\item{max_num_gene}{Integer. Maximum number of genes to return (default 50).}

\item{use_low}{Logical. Use robust lower-bound logFC instead of standard logFC (default TRUE).}

\item{n_cores}{Integer. Number of cores for parallel processing.}

\item{verbose}{Logical. Print progress.}
}
\value{
Updated \code{spaDesign2} object with \code{stable_genes} and \code{stable_gene_stats} populated.
}
\description{
Highly optimized version utilizing pre-filtering and vectorized operations
to bypass the computational bottleneck of gene summaries and nested loops.
}
\examples{
\dontrun{
data("mini_spaDesign2_obj", package = "spaDesign2")
spaDesign2_obj <- featureSelectionStable(
  object = mini_spaDesign2_obj,
  max_group_logfc_cut = 0.1,
  mean_global_cut = 0.1,
  min_domain_prop = 0.2,
  max_abs_logfc_cut = 0.1,
  cv_cut = 0.2,
  max_num_gene = 10,
  use_low = FALSE,
  n_cores = 8
)
}
}
