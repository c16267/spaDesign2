% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featureSelection.R
\name{featureSelection}
\alias{featureSelection}
\title{Select Domain-Informative Genes via Multi-Context Meta-Analysis}
\usage{
featureSelection(
  object,
  rep_sample_ids = NULL,
  logfc_cutoff = 0.5,
  mean_in_cutoff = 1,
  max_num_gene = 20,
  n_cores = 1,
  combine_method = c("union", "intersection"),
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{spaDesign2} object.}

\item{rep_sample_ids}{Character vector of representative samples. At least one
sample per condition should be provided. Defaults to the first sample if \code{NULL}.}

\item{logfc_cutoff}{Numeric. Minimum absolute log fold change (default: 0.5).}

\item{mean_in_cutoff}{Numeric. Minimum mean log-expression (default: 1.0).}

\item{max_num_gene}{Integer. Max top genes to extract per domain (default: 20).}

\item{n_cores}{Integer. CPU cores for parallel processing (default: 1).}

\item{combine_method}{Character. \code{"union"} (default) or \code{"intersection"}.}

\item{verbose}{Logical. Print progress.}
}
\value{
Updated \code{spaDesign2} object with integrated features.
}
\description{
Identifies robust marker genes by evaluating spatial expression across multiple
biological contexts (e.g., NAT vs. CRC). This meta-analysis mitigates representation
bias, ensuring the final feature space reflects the full transcriptomic heterogeneity.
}
\examples{
\dontrun{
data("mini_spaDesign2_obj", package = "spaDesign2")
spaDesign2_obj <- featureSelection(
  object = mini_spaDesign2_obj,
  logfc_cutoff = 0.5,
  mean_in_cutoff = 0.8,
  max_num_gene = 3,
  n_cores = 8
)
}
}
