% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/featureSelection.R
\name{makeCustomGeneSets}
\alias{makeCustomGeneSets}
\title{Create Custom Gene Sets for Simulation and Testing}
\usage{
makeCustomGeneSets(
  object,
  G_svg_base,
  G_stable,
  target_domain,
  reference_domain,
  sample_index = 1L,
  n_de = 5L,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{spaDesign2} object.}

\item{G_svg_base}{Character vector. Base marker genes known a priori.}

\item{G_stable}{Character vector. Stable genes identified via feature selection.}

\item{target_domain}{Character. The name of the target domain (e.g., "WM").}

\item{reference_domain}{Character. The name of the reference domain (e.g., "Layer6").}

\item{sample_index}{Integer or Character. The index or name of the pilot sample to use (default: 1L).}

\item{n_de}{Integer. Number of top differentially expressed genes to extract (default: 5L).}

\item{verbose}{Logical. Print progress and summary messages (default: TRUE).}
}
\value{
A list containing:
\item{G_svg}{Character vector. Union of \code{G_svg_base} and \code{G_stable}.}
\item{G_null}{Character vector. Unique stable genes.}
\item{G_spike}{Character vector. Top \code{n_de} differentially expressed genes.}
\item{TR_stats}{Data frame. Mean expressions and differences for all tested genes.}
}
\description{
Constructs customized gene sets (SVG, Null, and Spike) based on a specified
target and reference domain within a selected pilot sample. Identifies the
top differentially expressed genes from a pool of stable candidate genes.
}
\examples{
\dontrun{
data("mini_spaDesign2_obj", package = "spaDesign2")
G_svg_base <- mini_spaDesign2_obj@params_expression$top_genes
G_stable   <- mini_spaDesign2_obj@params_expression$stable_genes

# Construct the customized G_svg, G_null, and G_spike sets
sets <- makeCustomGeneSets(
  object = mini_spaDesign2_obj,
  G_svg_base = G_svg_base,
  G_stable = G_stable,
  target_domain = "WM",
  reference_domain = "Layer6"
)
}
}
