% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PowerEvaluation.R
\name{evaluatePowerSaLFC}
\alias{evaluatePowerSaLFC}
\title{Evaluate SaLFC Power over a Grid}
\usage{
evaluatePowerSaLFC(
  object,
  K_grid,
  lfc_grid,
  n_sim = 30,
  seed = 1,
  G_svg,
  G_null,
  G_spike,
  target_domain,
  reference_domain,
  eval_domain_col = c("hat_d", "domain"),
  rearrange = TRUE,
  jitter_frac = 0.3,
  k_nn = 100L,
  test_method = c("t", "wilcoxon"),
  adjust_method = c("bonferroni", "BH"),
  alternative = c("greater", "less", "two.sided"),
  alpha_fdr = 0.05,
  lfc_threshold = "auto",
  lfc_q = 0.75,
  spatial_mode = c("original", "basis"),
  n_cores = 1L,
  scenario_base = list(target_prop_case = NULL, delta_pp = NULL, kappa_m = 1, kappa_bio =
    1, delta_rho = 1, lambda_cond = 0.5),
  clustering_args = list(lambda = 0.5, k_neighbors = 20, scale_pilot_coords = FALSE,
    scale_sim_coords = FALSE, hard_guidance = FALSE, n_pcs = 20, do_hungarian = TRUE,
    use_G = TRUE, balance_mode = "energy_proportional"),
  tuning_obj = NULL,
  ...
)
}
\arguments{
\item{object}{A spaDesign2 object (fitted: pilot + params).}

\item{K_grid}{Numeric vector of samples per group (e.g. \code{seq(3, 8)}).}

\item{lfc_grid}{Numeric vector of effect sizes (DE_lfc, on the count-model
log scale used by \code{simulateSpaDesign2}).}

\item{n_sim}{Integer. Replications per grid point.}

\item{seed}{Integer. Master seed.}

\item{G_svg}{Character. All genes to simulate (\code{sets$G_svg}).}

\item{G_null}{Character. Null genes to evaluate (\code{sets$G_null}).}

\item{G_spike}{Character. True spike-in DE genes (\code{sets$G_spike}).}

\item{target_domain, reference_domain}{Domain labels.}

\item{eval_domain_col}{Label column for morphing AND testing: "hat_d"
(recovered clusters, default) or "domain" (ground truth).}

\item{rearrange}{Logical. Morph synthetic blobs onto pilot geometry first.}

\item{jitter_frac, k_nn}{Rearrangement tuning (used when \code{rearrange}).}

\item{test_method}{"t" or "wilcoxon".}

\item{adjust_method}{"BH" or "bonferroni" (default).}

\item{alternative}{SaLFC direction: "greater" (default), "less", "two.sided".}

\item{alpha_fdr}{Numeric. Rejection threshold for power/FDP.}

\item{lfc_threshold}{"auto" (pilot plug-in), a scalar, or a named vector.}

\item{lfc_q}{Numeric. Quantile for the auto threshold.}

\item{spatial_mode}{"original" or "basis".}

\item{n_cores}{Integer. Cores for simulate/cluster (keep modest under nesting).}

\item{scenario_base}{List. Base scenario (composition kept neutral by default
so the swept effect is purely expression DE).}

\item{clustering_args}{List. Passed to \code{pBANSKY} (must contain
\code{lambda}).}

\item{tuning_obj}{Optional \code{spaDesign2Tuning} object from
\code{\link{tuneSpaDesignLambdas}}. When supplied, the conditional-texture
weight \code{lambda_cond} (in the simulation scenario) and the clustering
weight \code{lambda} (in \code{clustering_args}) are taken \emph{per effect
size} from the matching row of \code{optimal_params$SaLFC$by_effect}
(matched on \code{DE_lfc}; nearest value with a warning if there is no exact
match), overriding the values in \code{scenario_base}/\code{clustering_args}.
Default \code{NULL}.}

\item{...}{Forwarded simulate tuning: \code{graph_k}, \code{gp_rank},
\code{gp_jitter}, \code{cond_k_nn}.}
}
\value{
A data.frame of per-replicate power/FDP rows, tagged with class
\code{"powerSaLFC"} (see \code{\link{print.powerSaLFC}},
\code{\link{summary.powerSaLFC}}, \code{\link{plotPowerCurveSaLFC}}).
}
\description{
Grid simulation over sample sizes (K) and effect sizes (DE_lfc) to evaluate
the power and FDP of the SaLFC endpoint. Per replicate the pipeline is
\code{simulateSpaDesign2()} -> \code{pBANSKY()} ->
\code{rearrangeSyntheticToPilot()} -> \code{SaLFC()}. Synthetic coordinates
are morphed onto the pilot domain geometry by \code{eval_domain_col}
("hat_d" by default), and SaLFC selects target/reference by the SAME column,
so the spatial variance \eqn{\tau^2} is computed on a consistent basis.
Pilot TREAT thresholds (\code{lfc_threshold = "auto"}) are estimated once per
DE_lfc and shared across replicates. A progress bar is shown and simulation
errors are skipped gracefully.
}
\seealso{
\code{\link{evaluatePowerLOR}}, \code{\link{SaLFC}},
\code{\link{tuneSpaDesignLambdas}}
}
