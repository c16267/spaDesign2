% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SaLFC.R
\name{estimate_lfc_threshold_from_pilot}
\alias{estimate_lfc_threshold_from_pilot}
\title{Estimate Gene-Specific SaLFC TREAT Thresholds from Pilot Data}
\usage{
estimate_lfc_threshold_from_pilot(
  object,
  genes,
  target_domain = "WM",
  reference_domain = "Layer6",
  rho_TR = 0,
  adjust_method = "bonferroni",
  test_method = "t",
  alternative = c("greater", "less", "two.sided"),
  q = 0.75,
  min_tau = 0,
  cap_effect = NULL,
  max_spots_per_domain = 5000L,
  verbose = TRUE,
  return_table = FALSE,
  test_fn = NULL,
  B = 200,
  shrink_to_global = 0.25,
  seed = 1
)
}
\arguments{
\item{object}{A spaDesign2 object with \code{@pilot_data}.}

\item{genes}{Character vector of (null) genes, e.g. the stable/null set.}

\item{target_domain}{Character. Target domain label.}

\item{reference_domain}{Character. Reference domain label.}

\item{rho_TR}{Numeric. Assumed Target-Reference biological correlation (< 1).}

\item{adjust_method}{Multiple-testing method passed through to SaLFC.}

\item{test_method}{SaLFC group comparison: "t" or "wilcoxon".}

\item{alternative}{Test direction: "greater" (default), "less", "two.sided".}

\item{q}{Numeric in (0,1). Standard-normal quantile level for \eqn{z_q}.}

\item{min_tau}{Numeric. Lower bound for \eqn{\tau_g}.}

\item{cap_effect}{Numeric scalar, named numeric vector, or NULL. Optional
upper cap on \eqn{\tau_g} (per gene if named).}

\item{max_spots_per_domain}{Integer. Per-sample spot cap before running SaLFC
on the pilot (bounds memory). The global RNG state is restored on exit.}

\item{verbose}{Logical.}

\item{return_table}{Logical. If TRUE return a data.frame, else a named vector.}

\item{test_fn}{Function. The SaLFC test to run on the pilot. If NULL, resolves
\code{SaLFC} by name. When invoked via
\code{SaLFC(..., lfc_threshold = "pilot")}, the
running test function is passed automatically, so renaming it does not break
this module.}

\item{B, shrink_to_global, seed}{Deprecated and ignored; retained for backward
compatibility.}
}
\value{
A named numeric vector \eqn{\tau_g} carrying an \code{attr(., "global_tau")},
or a data.frame when \code{return_table = TRUE}.
}
\description{
Fast plug-in estimator (no bootstrap) of gene-specific LFC thresholds
\eqn{\tau_g} for the TREAT-style SaLFC test, using only pilot data. SaLFC is
run once on the pilot samples with \code{lfc_threshold = 0}; \eqn{\tau_g} is
then defined from the pilot effect size and its uncertainty in a
direction-aware way:
\deqn{\tau_g = \max(\mathrm{effect\_ref}_g,\; z_q \cdot \mathrm{se}_g),}
where \eqn{\mathrm{effect\_ref}} depends on \code{alternative} and
\eqn{z_q = \Phi^{-1}(q)}.

This module is normally invoked automatically by
\code{SaLFC(..., lfc_threshold = "pilot")}, but it
can also be called directly and its result passed as \code{lfc_threshold}.
}
\seealso{
\code{\link{SaLFC}}
}
