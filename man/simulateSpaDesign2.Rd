% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulateSpaDesign2.R
\name{simulateSpaDesign2}
\alias{simulateSpaDesign2}
\title{Simulate Spatial Transcriptomics Data (Hybrid Mode, Optimized)}
\usage{
simulateSpaDesign2(
  object,
  n_sample_per_group = 10,
  scenario_settings = list(DE_lfc = 0, target_prop_case = NULL, delta_pp = NULL, kappa_m
    = 1, kappa_bio = 1, delta_rho = 1, lambda_cond = 0.1),
  target_domain = NULL,
  genes_to_simulate = NULL,
  de_genes = NULL,
  verbose = TRUE,
  n_cores = 4,
  gp_rank = 20L,
  gp_jitter = 1e-08,
  spatial_mode = c("original", "basis"),
  graph_k = 20L,
  cond_k_nn = 20L,
  basis_rank = 25L,
  basis_kernel = c("gaussian", "exponential"),
  basis_knot_method = c("sample", "grid"),
  basis_seed = 1L,
  lambda_cloud = NULL,
  seed_base = NULL
)
}
\arguments{
\item{object}{A \code{spaDesign2} object.}

\item{n_sample_per_group}{Integer. Number of synthetic samples to generate per group.}

\item{scenario_settings}{List. Scenario configurations including \code{DE_lfc},
\code{target_prop_case}, \code{kappa_m}, \code{kappa_bio}, \code{delta_rho},
and \code{lambda_cond}.}

\item{target_domain}{Character. Target domain name for differential effects.}

\item{genes_to_simulate}{Character vector. Genes to simulate.}

\item{de_genes}{Character vector. Subset of genes that will have DE effects applied.}

\item{verbose}{Logical. Print progress.}

\item{n_cores}{Integer. Number of cores for parallel execution.}

\item{gp_rank}{Integer. Rank for Gaussian Process (legacy).}

\item{gp_jitter}{Numeric. Jitter to ensure positive definiteness.}

\item{spatial_mode}{Character. "original" or "basis".}

\item{graph_k}{Integer. KNN for the whole-tissue spatial graph.}

\item{cond_k_nn}{Integer. KNN for the pilot-guided conditional texture.}

\item{basis_rank}{Integer. Basis rank for spatial mode "basis".}

\item{basis_kernel}{Character. "gaussian" or "exponential".}

\item{basis_knot_method}{Character. "sample" or "grid".}

\item{basis_seed}{Integer. Seed for basis knot sampling.}

\item{lambda_cloud}{Optional numeric in \code{[0, 1]}. Used for FGKMM simulation.}

\item{seed_base}{Optional integer for reproducible simulations.}
}
\value{
A list of simulated synthetic samples.
}
\description{
Optimized rewrite of \code{simulateSpaDesign2}. Statistically identical to the
previous version under the same \code{seed_base}, but substantially faster.
The main algorithmic gains include:
\itemize{
\item The conditional-texture (\code{eta_cond}) KNN structure is built
once per domain per sample and reused across all genes.
\item Sample/group invariants are hoisted out of the per-sample and per-gene loops.
\item The per-spot domain-mean lookup is fully vectorized.
}
}
\examples{
\dontrun{
# Load the pre-computed mini object and custom gene sets
data("mini_obj_fitted", package = "spaDesign2")
data("mini_custom_genes", package = "spaDesign2")

scenario_params <- list(
  DE_lfc = 0.0,
  target_prop_case = 0.7,
  kappa_geo = 1.0,
  kappa_m = 1.0,
  kappa_bio = 1.1,
  delta_rho = 1.0,
  lambda_cond = 0.5
)

sim_results <- simulateSpaDesign2(
  object = mini_obj_fitted,
  n_sample_per_group = 5,
  scenario_settings = scenario_params,
  target_domain = "WM",
  genes_to_simulate = mini_custom_genes$G_svg,
  de_genes = mini_custom_genes$G_spike,
  spatial_mode = "original",
  graph_k = 30L,
  verbose = TRUE,
  n_cores = 1
)
}
}
